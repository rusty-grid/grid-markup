// use grid_markup::parser;

// fn main() {
//     let file_path  = std::env::args().nth(1).expect("Missing arg");
//     let contents = std::fs::read_to_string(file_path).expect("Couldn't open file");
//     println!(
//         "{:#?}",
//         parser::parse(&contents).unwrap().1
//     )
// }

fn main() {}
